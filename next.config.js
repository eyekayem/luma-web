const { withNextVideo } = require("next-video/process");

/** @type {import('next').NextConfig} */
const nextConfig = {}; // Keep this empty or add any custom Next.js config here.

module.exports = withNextVideo(nextConfig);
